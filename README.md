# Maths-microproject!git config user.email "akr2006614@gmail.com"
!git config user.name "Akshay R"
!echo "# My repo" > README.md
!git add README.md
!git commit -m "Add README from Colab"
!git push https://{username}:{token}@github.com/{repo}.git HEAD:main
